export namespace CurrencyInfo {
    const AED: (string | number)[];
    const ALL: (string | number)[];
    const AUD: (string | number)[];
    const BDT: (string | number)[];
    const BGN: (string | number)[];
    const BRL: (string | number)[];
    const CAD: (string | number)[];
    const CDF: (string | number)[];
    const CHF: (string | number)[];
    const CLP: (string | number)[];
    const CNY: (string | number)[];
    const COP: (string | number)[];
    const CRC: (string | number)[];
    const CZK: (string | number)[];
    const DKK: (string | number)[];
    const DOP: (string | number)[];
    const EGP: (string | number)[];
    const ETB: (string | number)[];
    const EUR: (string | number)[];
    const GBP: (string | number)[];
    const HKD: (string | number)[];
    const HRK: (string | number)[];
    const HUF: (string | number)[];
    const IDR: (string | number)[];
    const ILS: (string | number)[];
    const INR: (string | number)[];
    const IRR: (string | number)[];
    const ISK: (string | number)[];
    const JMD: (string | number)[];
    const JPY: (string | number)[];
    const KRW: (string | number)[];
    const LKR: (string | number)[];
    const LTL: (string | number)[];
    const MNT: (string | number)[];
    const MVR: (string | number)[];
    const MXN: (string | number)[];
    const MYR: (string | number)[];
    const NOK: (string | number)[];
    const PAB: (string | number)[];
    const PEN: (string | number)[];
    const PHP: (string | number)[];
    const PKR: (string | number)[];
    const PLN: (string | number)[];
    const RON: (string | number)[];
    const RSD: (string | number)[];
    const RUB: (string | number)[];
    const SAR: (string | number)[];
    const SEK: (string | number)[];
    const SGD: (string | number)[];
    const THB: (string | number)[];
    const TRY: (string | number)[];
    const TWD: (string | number)[];
    const TZS: (string | number)[];
    const UAH: (string | number)[];
    const USD: (string | number)[];
    const UYU: (string | number)[];
    const VND: (string | number)[];
    const YER: (string | number)[];
    const ZAR: (string | number)[];
}
export namespace CurrencyInfoTier2 {
    const AFN: (string | number)[];
    const AMD: (string | number)[];
    const ANG: (string | number)[];
    const AOA: (string | number)[];
    const ARS: (string | number)[];
    const AWG: (string | number)[];
    const AZN: (string | number)[];
    const BAM: (string | number)[];
    const BBD: (string | number)[];
    const BHD: (string | number)[];
    const BIF: (string | number)[];
    const BMD: (string | number)[];
    const BND: (string | number)[];
    const BOB: (string | number)[];
    const BSD: (string | number)[];
    const BTN: (string | number)[];
    const BWP: (string | number)[];
    const BYN: (string | number)[];
    const BYR: (string | number)[];
    const BZD: (string | number)[];
    const CLF: (string | number)[];
    const CNH: (string | number)[];
    const CUC: (string | number)[];
    const CUP: (string | number)[];
    const CVE: (string | number)[];
    const DJF: (string | number)[];
    const DZD: (string | number)[];
    const ERN: (string | number)[];
    const FJD: (string | number)[];
    const FKP: (string | number)[];
    const GEL: (string | number)[];
    const GHS: (string | number)[];
    const GIP: (string | number)[];
    const GMD: (string | number)[];
    const GNF: (string | number)[];
    const GTQ: (string | number)[];
    const GYD: (string | number)[];
    const HNL: (string | number)[];
    const HTG: (string | number)[];
    const IQD: (string | number)[];
    const JOD: (string | number)[];
    const KES: (string | number)[];
    const KGS: (string | number)[];
    const KHR: (string | number)[];
    const KMF: (string | number)[];
    const KPW: (string | number)[];
    const KWD: (string | number)[];
    const KYD: (string | number)[];
    const KZT: (string | number)[];
    const LAK: (string | number)[];
    const LBP: (string | number)[];
    const LRD: (string | number)[];
    const LSL: (string | number)[];
    const LYD: (string | number)[];
    const MAD: (string | number)[];
    const MDL: (string | number)[];
    const MGA: (string | number)[];
    const MKD: (string | number)[];
    const MMK: (string | number)[];
    const MOP: (string | number)[];
    const MRO: (string | number)[];
    const MUR: (string | number)[];
    const MWK: (string | number)[];
    const MZN: (string | number)[];
    const NAD: (string | number)[];
    const NGN: (string | number)[];
    const NIO: (string | number)[];
    const NPR: (string | number)[];
    const NZD: (string | number)[];
    const OMR: (string | number)[];
    const PGK: (string | number)[];
    const PYG: (string | number)[];
    const QAR: (string | number)[];
    const RWF: (string | number)[];
    const SBD: (string | number)[];
    const SCR: (string | number)[];
    const SDG: (string | number)[];
    const SHP: (string | number)[];
    const SLL: (string | number)[];
    const SOS: (string | number)[];
    const SRD: (string | number)[];
    const SSP: (string | number)[];
    const STD: (string | number)[];
    const SYP: (string | number)[];
    const SZL: (string | number)[];
    const TJS: (string | number)[];
    const TMT: (string | number)[];
    const TND: (string | number)[];
    const TOP: (string | number)[];
    const TTD: (string | number)[];
    const UGX: (string | number)[];
    const UZS: (string | number)[];
    const VEF: (string | number)[];
    const VES: (string | number)[];
    const VUV: (string | number)[];
    const WST: (string | number)[];
    const XAF: (string | number)[];
    const XCD: (string | number)[];
    const XOF: (string | number)[];
    const XPF: (string | number)[];
    const ZMW: (string | number)[];
    const ZWD: (string | number)[];
}
/**
 * This function will add tier2 currency support. Be default, only tier1
 * (most popular currencies) are supported. If an application really needs
 * to support some of the rarely used currencies, it should call this function
 * before any other functions in this namespace.
 */
export function addTier2Support(): void;
/**
 * Modify currency pattern string by adjusting precision for given currency.
 * Standard currency pattern will have 2 digit after decimal point.
 * Examples:
 *   $#,##0.00 ->  $#,##0    (precision == 0)
 *   $#,##0.00 ->  $#,##0.0  (precision == 1)
 *   $#,##0.00 ->  $#,##0.000  (precision == 3)
 *
 * @param {string} pattern currency pattern string.
 * @param {string} currencyCode 3-letter currency code.
 * @return {string} modified currency pattern string.
 */
export function adjustPrecision(pattern: string, currencyCode: string): string;
/**
 * Deprecated.
 * Global currency pattern always uses ISO-4217 currency code as prefix. Local
 * currency sign is added if it is different from currency code. Each currency
 * is unique in this form. The negative side is that ISO code looks weird in
 * some countries as people normally do not use it. Local currency sign
 * alleviates the problem, but also makes it a little verbose.
 *
 * @param {string} currencyCode ISO-4217 3-letter currency code.
 * @return {string} Global currency pattern string for given currency.
 * @deprecated Format numbers using {@link goog.i18n.NumberFormat} with
 *   {@link goog.i18n.NumberFormat.Format.CURRENCY} and
 *   {@link goog.i18n.NumberFormat.CurrencyStyle.GLOBAL}
 */
export function getGlobalCurrencyPattern(currencyCode: string): string;
/**
 * Return global currency sign string for those applications
 * that want to handle currency sign themselves.
 *
 * @param {string} currencyCode ISO-4217 3-letter currency code.
 * @return {string} Global currency sign for given currency.
 */
export function getGlobalCurrencySign(currencyCode: string): string;
/**
 * Return global currency sign string for those applications
 * that want to handle currency sign themselves.
 *
 * This function does not throw an exception if there is no data for the
 * currency. Instead, it falls back to the ISO code.
 *
 * @param {string} currencyCode ISO-4217 3-letter currency code.
 * @return {string} Global currency sign for given currency.
 */
export function getGlobalCurrencySignWithFallback(currencyCode: string): string;
/**
 * Deprecated.
 * Local currency pattern is the most frequently used pattern in currency's
 * native region. It does not care about how it is distinguished from other
 * currencies.
 *
 * @param {string} currencyCode ISO-4217 3-letter currency code.
 * @return {string} Local currency pattern string for given currency.
 * @deprecated Format numbers using {@link goog.i18n.NumberFormat} with
 *   {@link goog.i18n.NumberFormat.Format.CURRENCY} and
 *   {@link goog.i18n.NumberFormat.CurrencyStyle.LOCAL}
 */
export function getLocalCurrencyPattern(currencyCode: string): string;
/**
 * Returns local currency sign string for those applications that need to
 * handle currency sign separately.
 *
 * @param {string} currencyCode ISO-4217 3-letter currency code.
 * @return {string} Local currency sign for given currency.
 */
export function getLocalCurrencySign(currencyCode: string): string;
/**
 * Returns local currency sign string for those applications that need to
 * handle currency sign separately.
 *
 * This function does not throw an exception if there is no data for the
 * currency. Instead, it falls back to the ISO code.
 *
 * @param {string} currencyCode ISO-4217 3-letter currency code.
 * @return {string} Local currency sign for given currency.
 */
export function getLocalCurrencySignWithFallback(currencyCode: string): string;
/**
 * Deprecated.
 * Portable currency pattern is a compromise between local and global. It is
 * not a mere blend or mid-way between the two. Currency sign is chosen so that
 * it looks familiar to native users. It also has enough information to
 * distinguish itself from other popular currencies in its native region.
 * In this pattern, currency sign symbols that has availability problem in
 * popular fonts are also avoided.
 *
 * @param {string} currencyCode ISO-4217 3-letter currency code.
 * @return {string} Portable currency pattern string for given currency.
 * @deprecated Format numbers using {@link goog.i18n.NumberFormat} with
 *   {@link goog.i18n.NumberFormat.Format.CURRENCY} and
 *   {@link goog.i18n.NumberFormat.CurrencyStyle.PORTABLE}
 */
export function getPortableCurrencyPattern(currencyCode: string): string;
/**
 * Return portable currency sign string for those applications that need to
 * handle currency sign themselves.
 *
 * @param {string} currencyCode ISO-4217 3-letter currency code.
 * @return {string} Portable currency sign for given currency.
 */
export function getPortableCurrencySign(currencyCode: string): string;
/**
 * Return portable currency sign string for those applications that need to
 * handle currency sign themselves.
 *
 * This function does not throw an exception if there is no data for the
 * currency. Instead, it falls back to the ISO code.
 *
 * @param {string} currencyCode ISO-4217 3-letter currency code.
 * @return {string} Portable currency sign for given currency.
 */
export function getPortableCurrencySignWithFallback(currencyCode: string): string;
/**
 * Tests if currency is available.
 *
 * Note: If the currency is not available it might be in the tier2 currency set:
 * {@link CurrencyInfoTier2}. If that is the case call
 * {@link addTier2Support} before calling any other function
 * in this namespace.
 *
 * @param {string} currencyCode Currency code to tested.
 * @return {boolean} If the currency is available.
 */
export function isAvailable(currencyCode: string): boolean;
/**
 * This function returns the default currency sign's position. Some applications
 * may want to handle currency sign and currency amount separately. This
 * function can be used in such situations to correctly position the currency
 * sign relative to the amount.
 *
 * Use {@link goog.i18n.NumberFormat#isCurrencyCodeBeforeValue} for a locale
 * aware version of this API (recommended). isPrefixSignPosition() returns the
 * default currency sign's position in the currency's default locale (e.g. 'en'
 * for 'USD'), but most commonly the position is needed for the locale in which
 * the number is going to be displayed. For example, in 'fr' 10.10 USD would be
 * displayed as '10,10 $'.
 *
 * @param {string} currencyCode ISO-4217 3-letter currency code.
 * @return {boolean} true if currency should be positioned before amount field.
 */
export function isPrefixSignPosition(currencyCode: string): boolean;
/**
 * Returns whether the string represents a valid ISO-4217 currency code.
 *
 * @param {string} currencyCode String to check.
 * @return {boolean} Whether currencyCode is a 3-letter currency code.
 */
export function isValid(currencyCode: string): boolean;
